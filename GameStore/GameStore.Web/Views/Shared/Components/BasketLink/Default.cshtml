@model int
@inject IViewLocalizer Localizer

<a class="menu-link" asp-action="IndexAsync" asp-controller="Basket">
    @Localizer["Cart"]
    @if (Model > 0)
    {
        <div class="d-inline-block">
            (<span class="total-items">@Model</span>)
        </div>
    }
</a>